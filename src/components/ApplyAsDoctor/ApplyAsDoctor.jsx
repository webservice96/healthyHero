import React from 'react';
import { Container } from 'react-bootstrap';

const ApplyAsDoctor = () => {
    return (
        <Container className="py-5">
            <h2>This is Doctor application!</h2>
        </Container>
    );
};

export default ApplyAsDoctor;